<view class="m-top2,content">

    <view wx:for="{{evaluate}}" wx:key="index" class="flex,m-top2,content-2">
         <view >
          <image src="{{item.author.avatar}}" class="image1"></image>
         </view>
         <view class="m-left2">
             <view class="nickname">{{item.author.nickname}}</view>
             <view class="content-1,m-top1">{{item.content}}</view>
         </view>
    
    </view>

</view>
